name: Ubuntu Latest CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make with gcc
      run: make CC=gcc
    - name: run tests
      run: make check
    - name: clean
      run: make clean
    - name: make with clang
      run: make CC=clang
    - name: run tests
      run: make check
    - name: sanitize with gcc
      run: make CC=gcc sanitize
    - name: sanitize with clang
      run: make CC=clang sanitize
    - name: asan leak check
      run: ASAN_OPTIONS=detect_leaks=1 make CC=gcc sanitize